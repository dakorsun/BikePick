import React from 'react';

/**
 * Displayed when there is no matching route
 */
const NotFound = () => (
    <div className="notfound">
        <h1 className="h1">We couldn't find the page you were looking for</h1>
        <p>I <strong>bet</strong> you weren't expecting that.</p>
    </div>
);

export default NotFound;
