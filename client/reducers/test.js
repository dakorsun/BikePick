import {

} from '../constants/actionTypes';

const INITIAL_STATE = {
    frame: '',
    stem: ''
};

export default (state = INITIAL_STATE, action = {}) => {
    switch (action.type) {

        default:
            return state;
    }
};
